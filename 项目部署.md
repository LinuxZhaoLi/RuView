
## 第一步：

安装：rustup

```
source "$HOME/.cargo/env"

sudo apt install git build-essential libwebkit2gtk-4.1-dev libgtk-3-dev libayatana-appindicator3-dev librsvg2-dev pkg-config libssl-dev -y
```



## 第二步

编译：sensing-server



```
./target/release/sensing-server --help
```



# WiFi-DensePose 感知服务器

用法：sensing-server [选项]

## 网络与端口相关

```
      --http-port <HTTP_PORT>
          UI界面与REST API的HTTP端口 [默认: 8080]
      --ws-port <WS_PORT>
          感知数据流WebSocket端口 [默认: 8765]
      --udp-port <UDP_PORT>
          ESP32 CSI帧数据的UDP端口 [默认: 5005]
      --udp-bind <UDP_BIND>
          CSI接收器UDP绑定地址（ADR-296规范）。默认127.0.0.1（仅本机回环）。
          若绑定局域网可路由地址（0.0.0.0或局域网IP），**必须搭配 --udp-allow 或 --udp-insecure-lan**
          [环境变量: RUVIEW_UDP_BIND=] [默认: 127.0.0.1]
      --udp-allow <IP/CIDR>
          入站UDP CSI帧的来源IP/网段白名单（逗号分隔，可多次写；环境变量 RUVIEW_UDP_ALLOW）。
          不在白名单内的数据包会被丢弃并计数。回环地址永远允许。
          示例：`--udp-allow 192.168.1.0/24,10.0.0.5`
      --udp-insecure-lan
          允许UDP绑定到公网/局域网地址，**显式接受局域网数据包伪造的安全风险（ADR-296）**。
          UDP数据平面本身无身份验证，详见仓库SECURITY.md安全文档 [环境变量: RUVIEW_UDP_INSECURE_LAN=]
      --bind-addr <BIND_ADDR>
          HTTP+WebSocket服务绑定地址（默认127.0.0.1；填0.0.0.0开启局域网访问）
          [环境变量: SENSING_BIND_ADDR=] [默认: 127.0.0.1]
      --no-mdns
          关闭本地 _ruview._tcp 设备发现。仅回环绑定会自动关闭发现；设备发现不会传输传感器数据
          [环境变量: RUVIEW_NO_MDNS=]
      --installation-id <INSTALLATION_ID>
          稳定的非保密部署标识，通过mDNS广播 [环境变量: RUVIEW_INSTALLATION_ID=]
      --installation-name <INSTALLATION_NAME>
          本地服务显示名称，用于配网客户端展示 [环境变量: RUVIEW_INSTALLATION_NAME=] [默认: "RuView Installation"]
      --allowed-host <HOST>
          额外允许的主机名（可带端口），用于Host头校验，防御DNS重绑定攻击。
          localhost、127.0.0.1这类回环地址默认放行。可多次添加，环境变量SENSING_ALLOWED_HOSTS支持逗号分隔。
      --disable-host-validation
          完全关闭Host头部校验。**仅在服务器前置反向代理（如Nginx）时使用**；裸机部署会存在DNS重绑定漏洞。
```

## UI、运行时

```
      --ui-path <UI_PATH>
          前端静态UI文件路径（仓库ui目录；在v2目录下可用../ui，也支持自动检测） [默认: ../ui]
      --tick-ms <TICK_MS>
          动画刷新间隔（毫秒），默认100毫秒 = 10帧/秒，用于姿态平滑渲染 [默认: 100]
```

## MQTT 相关（接入HomeAssistant等）

```
      --mqtt
          启用MQTT发布器，支持HomeAssistant自动发现 [环境变量: RUVIEW_MQTT=]
      --mqtt-host <MQTT_HOST>
          MQTT broker 服务器地址 [环境变量: RUVIEW_MQTT_HOST=] [默认: localhost]
      --mqtt-port <MQTT_PORT>
          MQTT端口（默认：明文1883 / TLS加密8883） [环境变量: RUVIEW_MQTT_PORT=]
      --mqtt-username <MQTT_USERNAME>
          MQTT用户名 [环境变量: RUVIEW_MQTT_USERNAME=]
      --mqtt-password-env <MQTT_PASSWORD_ENV>
          存放MQTT密码的环境变量名 [默认: MQTT_PASSWORD]
      --mqtt-client-id <MQTT_CLIENT_ID>
          MQTT客户端ID（默认：wifi-densepose-<进程ID>） [环境变量: RUVIEW_MQTT_CLIENT_ID=]
      --mqtt-prefix <MQTT_PREFIX>
          HA自动发现主题前缀（ADR-115 §9.2；仅支持homeassistant） [环境变量: RUVIEW_MQTT_PREFIX=] [默认: homeassistant]
      --mqtt-tls
          启用与MQTT服务器的TLS加密 [环境变量: RUVIEW_MQTT_TLS=]
      --mqtt-ca-file <PATH>
          TLS根证书文件路径
      --mqtt-client-cert <PATH>
          mTLS客户端证书
      --mqtt-client-key <PATH>
          mTLS客户端私钥
      --mqtt-refresh-secs <MQTT_REFRESH_SECS>
          HA设备发现信息刷新周期（秒） [默认: 600]
      --mqtt-rate-vitals <MQTT_RATE_VITALS>
          生命体征（心率/呼吸）发布频率(Hz) [默认: 0.2]
      --mqtt-rate-motion <MQTT_RATE_MOTION>
          运动状态发布频率(Hz) [默认: 1.0]
      --mqtt-rate-count <MQTT_RATE_COUNT>
          人数统计发布频率(Hz) [默认: 1.0]
      --mqtt-rate-rssi <MQTT_RATE_RSSI>
          WiFi信号强度RSSI发布频率(Hz) [默认: 0.1]
      --mqtt-publish-pose
          通过MQTT发布人体姿态关键点（默认关闭，占用带宽较高）
      --mqtt-rate-pose <MQTT_RATE_POSE>
          开启姿态发布后，姿态数据的推送帧率 [默认: 1.0]
      --privacy-mode
          MQTT/Matter发布前剥离生物特征（心率、呼吸、姿态）(ADR-115 §3.10) [环境变量: RUVIEW_PRIVACY_MODE=]
```

## 数据源 & 模型训练/转换

```
      --source <SOURCE>
          数据源：auto自动、wifi、esp32、simulate仿真模拟 [默认: auto]
      --benchmark
          运行生命体征检测基准测试（1000帧），测试完成直接退出
      --load-rvf <PATH>
          启动时从RVF容器加载模型配置
      --save-rvf <PATH>
          程序关闭时，将当前模型状态保存为RVF容器文件
      --model <PATH>
          加载训练好的.rvf推理模型
      --progressive
          开启渐进式加载（A层模型可瞬间启动）
      --export-rvf <PATH>
          导出RVF打包文件，导出完成直接退出（不启动服务）
      --convert-model <PATH>
          将模型文件（model.safetensors / model.rvf.jsonl）转换成RVF二进制容器，转换完退出 (#894)
          需要搭配 --convert-out 指定输出路径
      --convert-out <PATH>
          --convert-model 的输出文件路径（默认：<输入文件名>.rvf）
      --train
          进入训练模式，训练模型后退出
      --dataset <PATH>
          数据集目录
      --dataset-type <TYPE>
          数据集类型："mmfi" 或 "wipose" [默认: mmfi]
      --epochs <EPOCHS>
          训练轮次 [默认: 100]
      --checkpoint-dir <DIR>
          模型断点保存目录
      --pretrain
          运行自监督对比预训练（ADR-024）
      --pretrain-epochs <PRETRAIN_EPOCHS>
          预训练轮次 [默认: 50]
      --embed
          特征提取模式：加载模型，提取CSI嵌入特征向量
      --build-index <TYPE>
          根据提取的特征构建指纹索引，可选类型：env|activity|temporal|person
      --node-positions <NODE_POSITIONS>
          多节点融合的天线坐标，格式："x,y,z;x,y,z;..." [环境变量: SENSING_NODE_POSITIONS=]
      --calibrate
          开机启动现场模型校准（要求房间内无人）
```

## 边缘模块仓库

```
      --edge-registry-url <URL>
          上游边缘模块仓库地址。可填写镜像地址或本地file://路径用于离线部署。
          空字符串或--no-edge-registry可关闭该接口 [环境变量: RUVIEW_EDGE_REGISTRY_URL=]
          [默认: [https://storage.googleapis.com/cognitum-apps/app-registry.json](https://storage.googleapis.com/cognitum-apps/app-registry.json)]
      --edge-registry-ttl-secs <SECS>
          边缘仓库缓存有效期，单位秒 [环境变量: RUVIEW_EDGE_REGISTRY_TTL_SECS=] [默认: 3600]
      --no-edge-registry
          完全关闭边缘模块仓库接口。访问/api/v1/edge/registry直接返回404，用于离线隔离部署 [环境变量: RUVIEW_NO_EDGE_REGISTRY=]
```

## 帮助

```
  -h, --help
          打印帮助信息
```

------

# ✅ 你现在需要的启动命令（局域网可用 + ESP32 UDP接收）

> 重点：UDP绑定到0.0.0.0必须加 `--udp-insecure-lan`，这是代码强制安全校验！

```
./target/release/sensing-server \
--bind-addr 0.0.0.0 \
--udp-bind 0.0.0.0 \
--udp-insecure-lan
```

运行之后：

- HTTP：`[http://10.10.108.178:8080/ui/index.html](http://10.10.108.178:8080/ui/index.html)`
- WebSocket：`ws://10.10.108.178:8765/ws/sensing`
- ESP32 UDP发送目标：`10.10.108.178:5005`

> 可选：如果你只想限定ESP32来源网段（更安全，不使用insecure模式），例如ESP32在10.10.108.0/24网段：

```
./target/release/sensing-server \
--bind-addr 0.0.0.0 \
--udp-bind 0.0.0.0 \
--udp-allow 10.10.108.0/24
```

./crates/wifi-densepose-sensing-server/src/main.rs
./crates/cog-person-count/src/main.rs
./crates/ruv-neural/ruv-neural-cli/src/main.rs
./crates/wifi-densepose-cli/src/main.rs
./crates/homecore-migrate/src/main.rs
./crates/homecore-server/src/main.rs
./crates/cog-ha-matter/src/main.rs
./crates/wifi-densepose-desktop/src/main.rs
./crates/nvsim-server/src/main.rs
./crates/wifi-densepose-pointcloud/src/main.rs
./crates/cog-pose-estimation/src/main.rs